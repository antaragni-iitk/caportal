import { WheelTrackerDirective } from './wheel-tracker.directive';

describe('WheelTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new WheelTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
